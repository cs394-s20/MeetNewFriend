import React from 'react';
import {Button} from "rbx";
import { useState} from 'react';

const CreateEvent = ({closeModal}, {show}) => {
    return (
        <div> This is the content！！！ </div>
    );
};


export default CreateEvent;
